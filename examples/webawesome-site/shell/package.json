{
  "name": "trailhead-webawesome-example",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "description": "Example Trailhead shell with Web Awesome design system",
  "scripts": {
    "start": "npm run build && vite preview --port 3001",
    "dev": "vite",
    "build": "vite build && npm run copy-webawesome",
    "copy-webawesome": "rm -rf dist/webawesome && mkdir -p dist/webawesome && cp -R node_modules/@awesome.me/webawesome/dist-cdn/* dist/webawesome/",
    "preview": "vite preview"
  },
  "dependencies": {
    "@herdingbits/trailhead-core": "^0.4.0",
    "@herdingbits/trailhead-webawesome": "^0.4.0"
  },
  "devDependencies": {
    "@herdingbits/trailhead-types": "^0.4.0",
    "@types/node": "^26.1.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "@awesome.me/webawesome": "^3.12.0"
  }
}
